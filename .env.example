OJP_BASIC_AUTH_USER=
OJP_BASIC_AUTH_PASSWORD=
OJP_SOAP_ENDPOINT=
DATABASE_URL=postgresql://railsense:railsense@localhost:5432/railsense

# ── LLM backend ──────────────────────────────────────────────────────────────
# Local Ollama (default — no API key needed):
LLM_BASE_URL=http://localhost:11434/v1
LLM_MODEL=batiai/gemma4-e2b:q4

# Cloud OpenAI (or any OpenAI-compatible provider):
# Set LLM_API_KEY to switch to cloud mode automatically.
# LLM_API_KEY=sk-...
# LLM_BASE_URL=https://api.openai.com/v1   # optional override (default for cloud)
# LLM_MODEL=gpt-4o-mini
# ─────────────────────────────────────────────────────────────────────────────

RAILSENSE_DEBUG=false

# ── Staff authentication ──────────────────────────────────────────────────────
STAFF_USERNAME=staff
STAFF_PASSWORD=railsense123
# ─────────────────────────────────────────────────────────────────────────────
